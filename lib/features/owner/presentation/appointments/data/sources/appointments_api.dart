import 'package:dio/dio.dart';

class AppointmentsApi {
  final Dio dio;
  AppointmentsApi(this.dio);

  // TODO: Cuando el backend tenga endpoints (ej: /owner/appointments),
  // aquí van las llamadas reales.
}
